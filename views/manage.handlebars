<nav class="navbar fixed-top navbar-expand-lg navbar-dark">
    <a class="navbar-brand" href="/">
        <img class="logo" src="assets/images/favicon-02.png">
        <h1 id="appname">SitterFinder</h1>
    </a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
        aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav ml-auto">
            <li class="nav-item">
                <a class="nav-link" href="/search">find a sitter</a>
            </li>
            <li class="nav-item">
                <a id="manage" class="nav-link" href="/manage">my reservations</a>
            </li>
            {{#if user}} {{> navbar/nav-block}}
            {{else}}
            <li class="nav-item" id="signedinas">
            </li>
            <li class="nav-item">
                <button class="btn btn-pink auth-btn" id="barloginbtn">log in</button>
            </li>
            <li class="nav-item">
                <button class="btn btn-purple auth-btn" id="barregisterbtn">register</button>
            </li>
            {{/if}}
        </ul>
    </div>
</nav>
<div class="splash">
    <div class="container-fluid">
        <div class="row justify-content-center">
            <div class="col-sm-11 col-md-8">
                <h3>My Reservations</h3>
                {{#each reservations}}
                {{#unless cancelled}}
                {{> reservations/res-block}}
                {{/unless}}
                {{/each}}
            </div>
        </div>
    </div>
</div>
<div id="reservation-modal" class="modal">
    <div class="row justify-content-center">
        <div class="modal-content col-11 col-sm-11 col-md-9 col-lg-6 col-xl-5">
            <span class="close">&times;</span>
            <h3 id="resdeletemessage">Reservation Deleted</h3>
        </div>
    </div>
</div>